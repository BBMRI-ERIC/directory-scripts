Install:

* [Cygwin] python3 -m ensurepip

* pip install networkx
  or 
  pip3 install networkx
  or
  pip install --upgrade networkx

* pip install validate_email
  or similar to above

* pip install xlsxwriter
  or similar to above

* pip install py3dns
  or similar to above
  and on Cygwin create /etc/resolv.conf

* pip install diskcache
  or similar to above and
  mkdir -p data-quality-cache/directory
  mkdir -p data-quality-cache/geolocator
  mkdir -p data-quality-cache/emails

* wget https://directory.bbmri-eric.eu/molgenis.py
