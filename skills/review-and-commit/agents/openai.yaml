interface:
  display_name: "Review and Commit"
  short_description: "Run pre-commit review, validation, and commit"
  default_prompt: "Use $review-and-commit to review current changes, run validations, verify required runtime files are tracked, and commit at the end."
